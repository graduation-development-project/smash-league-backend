services:
  api:
    container_name: smash_league_api
    ports:
      - 5000:5000
    environment:
      - DATABASE_URL=postgres://root:password@database:5432/smashleague?schema=public
      - PREFIX=api/v1
      - ACCESS_TOKEN_SECRET_KEY="167031@SmashLeague@ACCESS"
      - REFRESH_TOKEN_SECRET_KEY="167031@SmashLeague@REFRESH"
      # Access Token
      - JWT_ACCESS_TOKEN_EXPIRATION_TIME=1800
      - JWT_REFRESH_TOKEN_EXPIRATION_TIME=25200
      #Refresh token
      - RESET_TOKEN_SECRET_KEY="167031@SmashLeague@RESET"
      - RESET_TOKEN_EXPIRATION_TIME=600
      #Reset Password URL
      - EMAIL_RESET_PASSWORD_URL="http://localhost:3000/auth/reset-password"
      #Cloudinary
      - CLOUDINARY_NAME="dqdggrw7o"
      - CLOUDINARY_API_KEY="172175352464367"
      - CLOUDINARY_API_SECRET="63nzUkonqm4rGsB5mHKIXRAcp5s"
      #NODEMAILER
      - NODEMAILER_USER=sonpv07@gmail.com
      - NODEMAILER_PASS=klcpbigrohbrxedh
      #MAILER_TEMPLATE_URL
      - MAILER_TEMPLATE_URL=infrastructure/email-templates
      #PAYOS API KEY
      - PAYOS_CLIENT_ID="75ef8838-9823-4044-a2a2-d12bd066eb51"
      - PAYOS_API_KEY="555d3f78-3980-40e5-8e5f-dbb30f22cd94"
      - PAYOS_CHECKSUM_KEY="7e1a6d408a2bfc81ea39c49c659b0bbb2397a1babfca676924f959f89980173e" 
    build: 
      context: .
      dockerfile: Dockerfile
    command: bash -c "npx prisma migrate deploy; npx prisma generate; npm run build:linux; npm run start:prod"
    # command: bash -c "npx prisma migrate resolve --applied 20250201055158_init_db && node dist/main.js"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - db
    networks:
      - smashleague
  db:
    container_name: database
    image: postgres:17.2
    restart: always
    ports: 
      - "5002:5432"
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      POSTGRES_DB: smashleague
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - smashleague

networks:
  smashleague: {}
volumes:
  postgres_data: 
